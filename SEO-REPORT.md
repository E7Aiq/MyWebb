# تقرير: تهيئة الظهور في البحث — alzobaidi.me

**الحالة:** العمل كامل ومُودَع على فرع `seo/static-prerender`. **لم يُنشَر شيء.**
الموقع الحيّ اليوم هو نفسه قبل هذا العمل، حرفاً بحرف، حتى تقرأ هذا وتقرّر.

**المشكلة التي عولجت:** صفحتا الفهرس وصفحتا التفاصيل كنّ لا يُخرجن محتوى بلا
جافاسكربت. ما كان يراه واتساب ولينكدإن وإكس وتلغرام وبينغ وزواحف نماذج اللغة
عند فتح رابط مقال هو **٦٩ محرفاً** نصّها: «جاري تحميل المقال... عذراً! لم
نتمكن من تحميل المقال. العودة للمقالات». صار المقال نفسه **٢٧٤٥ محرفاً** في
الترميز الخام. هذا مقيسٌ بـ`curl` على الحالتين، لا مقدَّر.

---

# الجزء ١ — ما عُمل

## ١.١ التصيير المسبق (الأهمّ)

طبقة بناء جديدة تُشغَّل **بعد** جالبَي Notion ولا تمسّهما، تقرأ `data/*.json`
وتكتب صفحات HTML حقيقية.

| الملف | ماذا |
|---|---|
| `.github/scripts/build-site.js` | المحرّك: يقرأ البيانات ويكتب كل صفحة وخريطة وتغذية وبطاقة |
| `.github/scripts/lib/seo.js` | الاشتقاق: الوصف من المتن، العنوان، المعرّف، التاريخ، زمن القراءة |
| `.github/scripts/lib/chrome.js` | الرأس والنافبار والتذييل وفتات الخبز — **منقولة حرفياً** عن الصفحات اليدوية |
| `.github/scripts/lib/og.js` | بطاقات المشاركة ١٢٠٠×٦٣٠ |
| `.github/scripts/lib/share-icons.js` | أيقونات المشاركة، نفسها في المسارين |
| `.github/scripts/check-seo.js` | الحارس — يوقف البناء عند أي انحدار |

**ما يُنتَج:** صفحة لكل مقال ومشروع، الفهرسان، فهرس الموضوعات وصفحة لكل
موضوع، خريطة الموقع، تغذية Atom، `robots.txt`، بطاقة مشاركة لكل صفحة، وجسور
تحويل لكل مسار قديم.

**الكنس:** ما لم يعد له مصدر في `data/` يُحذف. إلغاء نشر مقال في Notion كان
سيترك صفحته مقدَّمةً على الخادم إلى الأبد؛ لم يعد كذلك.

**التحقّق:** `curl` على كل نوع صفحة، بلا جافاسكربت:

| المسار | نصّ المتن قبل | بعد |
|---|---|---|
| صفحة مقال | ٦٩ محرفاً (رسالة خطأ) | **٢٧٤٥** |
| صفحة مشروع | ٩٣ محرفاً (رسالة خطأ) | **٥٦٧١** |
| فهرس المقالات | ٢٩٦ (الماستهيد وحده، صفر مقال) | ٤٥٤ (ومعه المقالان) |
| فهرس المشاريع | ٥١٨ (صفر مشروع) | ٩١٠ (ومعه المشروعان) |

## ١.٢ بيانات وصفية لكل صفحة

عنوان فريد، ووصف فريد ١٥٠–١٦٠ محرفاً، ورابط معياري مطلق، وطقم Open Graph
كامل (`og:type`/`title`/`description`/`image`/`image:width`/`image:height`/
`locale`/`article:published_time`/`article:modified_time`/`article:tag`)،
و`twitter:card = summary_large_image` بحقوله المطابقة.

**الوصف مشتقّ لا مخترَع:** حقلا `description` و`summary` فارغان في كل عنصر
اليوم، فيُقتطع الوصف من أول نثر حقيقي في المتن ويُقطع عند حدّ جملة. اقتباسات
الترويسة تُستبعد صراحةً — بدونها كان وصف أول مشروع سيصير
«Project Owner: Mohammed Abdullah Alzobaidi · Status: Done!» في نتيجة البحث.

**بطاقة المشاركة:** إن وُجد غلاف فهو البطاقة (يُقصّ ١٢٠٠×٦٣٠ ويُحوَّل إلى
JPEG — المصدر WebP وعارضات المعاينة لا يُعوَّل على دعمها له). وإلّا تُرسَم
بطاقة من العنوان بخطّ ثمانية ورموز الموقع عبر `resvg` (تشكيل عربي حقيقي).
**الصورة الشخصية لا تقع بطاقةً لمقال أبداً.** الحصيلة: ٤ بطاقات من أغلفة
حقيقية و١١ مرسومة.

## ١.٣ البيانات المنظّمة

`Person` و`WebSite` على الرئيسية · `BlogPosting` لكل مقال · `CreativeWork`
لكل مشروع · `CollectionPage` + `ItemList` للفهارس وصفحات الموضوعات ·
`BreadcrumbList` على كل صفحة فرعية. ٣٠ كتلة، كلّها تُحلَّل وتمرّ فحص الحقول
الواجبة في `check-seo.js`.

**`SearchAction` لم يُصدَر** لأن الموقع بلا بحث داخليّ؛ التصريح به بلا مسار
بحث حقيقي إعلانُ ميزة غير موجودة.

## ١.٤ سطح الموضوعات

صفحة لكل موضوع (٧ اليوم، كلّها من تصنيفات المشاريع) بعنوانها ووصفها ورابطها
المعياري، تجمع كل مقال ومشروع تحتها. ومحتوى مرتبط في تذييل كل صفحة، **عبر
النوعين وفي الاتجاهين**: مقال موسوم `Power BI` يظهر في صفحة المشروع، والعكس.

`/topics/` مبلوغ من فتات خبز صفحات الموضوعات، وهي مبلوغة من تذييلات المقالات
والمشاريع، وهي مبلوغة من الفهرسين، وهما من الرئيسية. **صفر صفحة يتيمة** —
مفحوصٌ آلياً بمسح عرضيّ للرسم البياني من `/`.

## ١.٥ بنية الزحف

- `sitemap.xml` مولَّدة كل بناء: ١٥ رابطاً بتواريخ `lastmod` حقيقية من
  `last_edited`. وتنقسم تلقائياً إلى ملفات وفهرسٍ يجمعها متى تجاوزت خمسة
  آلاف رابط — **وهذا الفرع مُشغَّل ومفحوص فعلاً**، لا شفرةً نائمة: خُفِّض
  الحدّ إلى ٥ عبر `SITEMAP_MAX` فانقسمت إلى ثلاثة ملفات وفهرس، ثم أُعيد
  الحدّ فكُنست الثلاثة وعاد الملف الواحد.
- `robots.txt` يسمح صراحةً لزواحف البحث **ولزواحف نماذج اللغة** (GPTBot،
  ClaudeBot، PerplexityBot، Google-Extended، CCBot…) — صاحب الموقع يستفيد من
  أن يُقتبس.
- `feed.xml` تغذية Atom لبان بالمتن الكامل، موصولة بـ`rel="alternate"` من كل
  صفحة.
- `404.html` كانت **مكسورة خارج الجذر**: مسارات أصولها نسبيّة، فصفحة الخطأ
  على `/articles/xyz/` كانت تُقدَّم بلا أنماط ولا خطوط. جُذِّرت.
- صيغة مضيف واحدة: `https://alzobaidi.me` بلا `www`، بشرطة مائلة ختامية،
  والجذر `/` لا `/index.html`.

## ١.٦ المسارات النظيفة

`/articles/<معرّف>/` و`/projects/<معرّف>/`. وكل مسار قديم يبقى عاملاً:

| المسار القديم | كيف عولج | مفحوص |
|---|---|---|
| `/articles.html` · `/projects.html` | `canonical` + `meta refresh` + إعادة توجيه فورية | ✓ |
| `/article.html?id=…` · `/project-details.html?id=…` | خريطة المعرّفات مضمَّنة في الترميز (بلا طلب شبكة)، وتحتها قائمة روابط كاملة في `<noscript>` — ممرّ زحف لا طريق مسدود | ✓ الأربعة |
| عنوان تغيّر في Notion | `data/slugs.json` يحفظ المعرّف الأول ويكتب جسراً للقديم تلقائياً | ✓ آلياً |

## ١.٧ الأداء وسهولة الوصول

- الخطوط: التحميل المسبق و`font-display: swap` كانا قائمين — لم يُمسّا.
- **أبعاد صريحة** لكل صورة في المتن، مقروءة من الملف نفسه وقت البناء.
- كسول لما تحت الحدّ، والغلاف (عنصر LCP) لا يُحمَّل كسولاً أبداً.
- **النصّ البديل:** Notion يضع اسم الملف نصّاً بديلاً حين لا تعليق. اسم ملف
  يُنطَق حرفاً حرفاً في قارئ الشاشة أسوأ من الصمت — فتُعلَن الصورة زخرفية
  `alt=""` ويُبلَّغ بعددها (٦ صور اليوم) بدل اختراع وصف.
- شريط الأدوات: النسخ المكرّرة كانت `aria-hidden` أصلاً — تُحقّق منه ولم يُمسّ.
- **عنوان واحد من المستوى الأول لكل صفحة.** عناوين متن Notion تُنزَّل إلى
  `h2` — والمقياس لا يتغيّر لأن `.article-body h1` و`h2` متطابقتان في CSS
  عائلةً وحجماً ووزناً (مُتحقَّق منه في المصدر قبل التغيير).
- `<article>` و`<time datetime>` و`<nav>` في كل صفحة مولَّدة.
- فحص Lighthouse: **الوصول ١٠٠، الممارسات ١٠٠، SEO ١٠٠** على كل صفحة، قبل
  وبعد.
- **فحص شامل في متصفّح حقيقي: ٢٤ صفحة × ٣ عروض (٣٧٥ · ٩٠٠ · ١٢٨٠) = ٧٢
  فحصاً** — لا على عيّنة. النتيجة النهائية: صفر تجاوز أفقي، صفر اتجاه خاطئ،
  صفر صورة بلا أبعاد أو مكسورة، صفر خطأ طرفية، صفر طلب فاشل.

## ١.٨ الديمومة

- `docs/SEO.md`: الخطّ كاملاً، وجدول «أيّ حقل في Notion يغذّي أيّ وسم»، وما
  يجب فعله عند إضافة مقال.
- `check-seo.js` موصول بخطوة البناء في **كلا** مسارَي Notion، فلا تصل صفحة
  معطوبة إلى النشر. متاح أيضاً بـ`npm run check`.
- الحلقة كاملة: مقال جديد في Notion ⇒ صفحته + بياناته + بطاقته + مدخله في
  الخريطة والتغذية + روابطه الداخلية، **بلا خطوة يدوية واحدة**.

## ١.٩ الملفات

**جديدة:** `.github/scripts/build-site.js` · `.github/scripts/check-seo.js` ·
`.github/scripts/lib/{seo,chrome,og,share-icons}.js` · `data/site.json` ·
`data/slugs.json` · `docs/SEO.md` · `package.json` · `.gitignore` ·
`.nojekyll` · `feed.xml` · `articles/**` · `projects/**` · `topics/**` ·
`a/**` · `p/**` · `assets/og/**`

**معدَّلة:** `index.html` (مناطق مُدارة + مسارات نظيفة) · `404.html` (تجذير
المسارات) · `articles.html`/`projects.html`/`article.html`/
`project-details.html` (صارت جسوراً) · `sitemap.xml` · `robots.txt` ·
`main.js` (الرابط النشط بالقسم لا بالملف) · `js/covers.js` · `js/article.js` ·
`js/project.js` · `js/articles-list.js` · `projects.js` ·
`js/view-transitions.js` · `css/style.css` (فتات الخبز) · `css/article.css`
(محدِّدان) · كلا ملفَّي سير العمل.

**لم تُمسّ:** `.github/scripts/fetch-articles.js` · `fetch-projects.js` ·
`data/articles.json` · `data/projects.json` · `css/project.css` ·
`css/responsive.css` · `css/animations.css` · `animations.js` · `effects.js` ·
`js/i18n.js` · كل الخطوط والصور.

---

# الجزء ٢ — قرارات اتُّخذت بلا سؤال

### ١. المعرّف العربي — والقرار تجريبيّ كما طُلب

**القرار: المعرّف يتبع خطّ العنوان.** عنوان عربي ⇒ معرّف عربي؛ عنوان لاتيني
⇒ معرّف لاتيني. ولا نقحرة: «الذكاء الاصطناعي» تصير `aldhka-alastnay`، وهي
ليست كلمةً في أي لغة — تخسر معنى العنوان ولا تكسب قابلية قراءة.

**ما قِسته فعلاً:**

| الفحص | النتيجة |
|---|---|
| المسار المرمَّز بالنسبة عبر HTTP | ‏200 ✓ |
| المسار بـUTF-8 خام عبر HTTP | ‏200 ✓ |
| دورة `encodeURIComponent`/`decode` | مطابقة تامّة ✓ |
| وسوم OG بوكيل WhatsApp/Facebook/LinkedIn/Twitter/Slack | ٣/٣ لكلٍّ ✓ |
| **طول الرابط** | **٢٢٢ محرفاً** مرمَّزاً · ٧٤ للاتيني · **٣٢ للرابط القصير** |

**ما لم أستطع قياسه:** سلوك تطبيق واتساب نفسه عند لصق الرابط — يحتاج حساباً
وجهازاً. لم أدّعِ فحصه. (المهمّة ٦ في الجزء ٤.)

**التحوّط:** لأن هذا الطرف وحده غير مقيس، لكل عنصر **رابط قصير لاتينيّ**
`/a/<٨ محارف>/` يحمل بطاقة المشاركة كاملةً ثم يحوّل إلى المسار النظيف —
٣٢ محرفاً بدل ٢٢٢. فلا يكون أيّ سطح مشاركة نقطة فشل وحيدة، مهما كانت نتيجة
الفحص اليدوي.

### ٢. مبدّل EN: تبديل نصوص في المتصفّح فقط — فلا `hreflang`

**مفحوص في المتصفّح، لا مقروءاً من الشفرة:** عند الضغط على EN يتغيّر
`<html lang>` و`dir`، وتُبدَّل تسميات الواجهة (الرئيسية ← Home، بان — المقالات
← Ban — Writing، شارك المقال ← Share this article)، ويُعاد تنسيق التاريخ
(٩ سبتمبر ٢٠٢٣ ← 9 September 2023)، ويُحفظ الاختيار في `localStorage`.

وفي المقابل: **الرابط لا يتغيّر · `<title>` يبقى عربياً · `<h1>` يبقى عربياً ·
متن المقال يبقى عربياً · عدد روابط `hreflang` = صفر.**

فلا صفحات إنجليزية. ولذلك **لم أُضِف `hreflang`** — إضافته بلا صفحة مقابلة
حقيقية تصريحٌ كاذب لمحرّك البحث ويُبلَّغ عنه خطأً في Search Console. التوصية
في الجزء ٥.

### ٣. فتات الخبز — العنصر المرئي الوحيد المضاف

طُلب صراحةً (بند ٤) وطُلب أن يطابق `BreadcrumbList`. أضفته على الصفحات
الفرعية فقط، بخطّ الواجهة ومقياس الميتا ولون النصّ الخافت — **بلا رمز جديد
ولا لون جديد ولا مقاس خارج السلّم**. والفاصل «·» هو فاصل الموقع المعتمد أصلاً
(`.category-tag` في `project.css`)، وهو محايد الاتجاه فلا يقلبه bidi كما
تُقلب الأسهم. يشغل ~٤٣.٥px. هذا هو الفارق البصري الوحيد على تلك الصفحات.

### ٤. الموضعة: وُصِّلت ولم تُحسَم

طُلب ألّا أختار. `data/site.json` يحمل الآن `search_role_ar` و`hero_role_ar`
منفصلين، **كلٌّ بقيمته الحالية بالضبط** — فما يظهر في نتائج البحث لم يتغيّر
بحرف. البناء يطبع تحذيراً، والفاحص يطبع تحذيراً، حتى تُوحَّدا. المهمّة ١.

### ٥. الجسور بالمعرّف: خريطة مضمَّنة، لا `meta refresh` واحد

`article.html?id=…` ملفٌّ واحد لمئة معرّف، فلا يمكن أن يحمل `meta refresh`
لكلٍّ منها. الخريطة تُولَّد في البناء وتُضمَّن في الترميز، فالتحويل بلا طلب
شبكة. وتحتها قائمة روابط كاملة تحت `<noscript>`: زاحفٌ لا ينفّذ جافاسكربت
يجد ممرّاً إلى كل مقال بدل جدار. والصفحة `noindex, follow` لأنها بلا محتوى
خاصّ بها.

### ٦. بطاقة الرئيسية: مرسومة، مع مفتاح لاستبدالها

**قرار كان يجب أن أصرّح به في أول تسليم ولم أفعل:** `og:image` للصفحة
الرئيسية كان `assets/images/profile.jpg` وصار بطاقة مرسومة.

**لماذا:** الصورة الشخصية ٦٤٠×٨٥٧ عمودية. بطاقة المشاركة ١٢٠٠×٦٣٠ أفقية،
وقصّ الأولى إلى الثانية يقطع الرأس. فالبطاقة المرسومة (الاسم + الدور بخطّ
ثمانية) أسلم افتراضاً.

**وهو قرارك لا قراري**، فأضفت له مفتاحاً: `home.og_image` في
`data/site.json`. `null` ⇒ مرسومة. ضع فيه مسار صورة ١٢٠٠×٦٣٠ ⇒ تُستعمل كما
هي. القاعدة الأخرى لا تتغيّر: **لا صورة شخصية بطاقةً لمقال، أبداً.**

### ٧. `.nojekyll`

أُضيف. الموقع يُقدَّم من فرع، وبناء Jekyll الافتراضي لا يضيف شيئاً هنا ويضيف
احتمال أن يعبث بمسار. حتميّة أعلى وبناء أسرع.

### ٨. المسارات مطلقة في الصفحات المولَّدة

الصفحات صارت في أدلّة فرعية، والمسار النسبي كان سيُحلّ إلى
`/articles/<slug>/css/style.css`. جُذِّرت في المولَّد، وفي `js/covers.js`،
وفي عناوين البيانات في `articles-list.js` و`projects.js`.

### ٩. سجلّ المعرّفات `data/slugs.json`

تغيير عنوان في Notion كان سيغيّر مسار الصفحة ويكسر رابطاً منشوراً. السجلّ
يثبّت المعرّف الأول ويكتب جسراً للقديم تلقائياً.

### ١٠. `git pull --rebase` قبل الدفع في كلا المسارين

المساران يعملان على `0 3 * * *` معاً، وصارا يكتبان الملفّات المولَّدة نفسها.
بلا إعادة أساس، يفشل دفع أحدهما بـ`non-fast-forward`.

---

# الجزء ٣ — النتائج المقيسة

## ٣.١ التصميم: الرئيسية مطابقة بايتاً ببايت

لقطات كاملة الصفحة (1280×5200، Chrome بلا واجهة، `--force-prefers-reduced-motion`)
للنسختين، ثم مقارنة بكسل ببكسل:

| الصفحة | البكسلات المختلفة | التفسير |
|---|---|---|
| **الرئيسية** | **٠ من ٦٬٦٥٦٬٠٠٠ (٠٫٠٠٠٠٪)** | مطابقة تامّة — ويشمل ذلك قسم «مختارات» الذي انتقل من البناء بجافاسكربت إلى التصيير المسبق |
| فهرس المقالات | إزاحة ٤٤px، والمتبقّي بعدها ٠٫٢٨٪ | فتات الخبز، وباقي الفرق تنعيم حواف بفارق بكسل |
| فهرس المشاريع | إزاحة ٤٤px، والمتبقّي ٠٫٤٠٪ | كما أعلاه |
| صفحة مقال | إزاحة ٤٣px، والمتبقّي ٢٫٤٪ | كما أعلاه؛ وبعض النطاقات مطابقة تماماً (٠ بكسل) |
| صفحة مشروع | إزاحة ٤٣px، والمتبقّي ٢٫٥٪ | كما أعلاه |

«المتبقّي» ليس تغييراً في التصميم: بعد تعويض إزاحة فتات الخبز، تنطبق النطاقات
انطباقاً تامّاً في بعضها (٠ بكسل)، والباقي فرق بكسل واحد في تنعيم الحواف لأن
ارتفاع فتات الخبز كسريّ (~٤٣٫٥px). فُحص بصرياً على قصاصات من الترويسة والمتن
والتذييل: متطابقة.

> **الرئيسية صفر بكسل** هو الرقم الحاسم: هي الصفحة الوحيدة التي لم يُضَف إليها
> فتات خبز، وقد تغيّرت آليّة بناء قسم كامل فيها — ومع ذلك لم يتحرّك بكسل واحد.

**عطب اكتُشف أثناء هذا الفحص:** الفارق الأول على الرئيسية كان ٢٩٧ بكسلاً في
النافبار — رابط «الرئيسية» فقد حالة النشاط. السبب: `setActiveNavLink` كانت
تطابق آخر جزء من المسار باسم ملف، والمسار صار `/`. أُعيدت كتابتها لتطابق
بالقسم. بعد الإصلاح: صفر.

## ٣.٢ Lighthouse

قياس محلّي (خادم بايثون متعدّد الخيوط)، Lighthouse 12.8.2، محاكاة الجوال.
**الرئيسية بثلاث جولات لكل جانب** لأن الجولة الواحدة تتأرجح ±١٠ نقاط:

| الصفحة | الأداء (وسيط ٣ جولات) | الوصول | الممارسات | SEO | CLS |
|---|---|---|---|---|---|
| الرئيسية — قبل | ٦٨ ‏(٦٨، ٦٨، ٧٨) | ١٠٠ | ١٠٠ | ١٠٠ | ٠ |
| الرئيسية — بعد | ٦٨ ‏(٧٣، ٦٨، ٦٨) | ١٠٠ | ١٠٠ | ١٠٠ | ٠ |
| صفحة مقال — قبل | ٦٦ | ١٠٠ | ١٠٠ | ١٠٠ | **٠٫١٤٧** |
| صفحة مقال — بعد | ٦٨ | ١٠٠ | ١٠٠ | ١٠٠ | **٠** |
| فهرس المقالات — قبل | ٧٣ | ١٠٠ | ١٠٠ | ١٠٠ | ٠ |
| فهرس المقالات — بعد | ٧١ | ١٠٠ | ١٠٠ | ١٠٠ | ٠ |

**قراءة صادقة للأرقام:**

- **المكسب الحقيقي والوحيد المؤكَّد: CLS على صفحة المقال ٠٫١٤٧ ← ٠.** وهو
  مؤشّر ويب أساسي، وحتميّ لا عشوائيّ: زال لأن المحتوى صار في الترميز ولم يعد
  يُحقن بعد الرسم، ولأن كل صورة صارت تحمل أبعادها.
- **الأداء لم يتغيّر بمعنى يُعتدّ به.** الفارق داخل نطاق الضجيج (±١٠ نقاط بين
  جولتين على الجانب نفسه). أول قياس أجريته أعطى ٨٥ ← ٦٨ وبدا انحداراً؛ تبيّن
  أنه أثر خادم بايثون أحاديّ الخيط يُسلسِل الطلبات. أعدته على خادم متعدّد
  الخيوط بثلاث جولات: الوسيط ٦٨ للجانبين.
- **رقم الأداء المحلّي غير ممثِّل للإنتاج أصلاً** — بلا ضغط ولا CDN ولا
  زمن شبكة. الرقم المُعتدّ به هو من PageSpeed Insights على الرابط الحيّ.
  (المهمّة ٧.)
- الوصول والممارسات وSEO: ١٠٠/١٠٠/١٠٠ قبل وبعد. كانت ممتازة، وبقيت.

## ٣.٣ العدد والانكشاف

| | قبل | بعد |
|---|---|---|
| ملفات HTML | ٦ | ٢٤ (١٥ صفحة محتوى · ٨ جسور · ٤٠٤) |
| روابط خريطة الموقع | ٧ (٤ منها بمعامل استعلام) | ١٥ (كلّها مسارات نظيفة) |
| صفحات لها عنوان فريد | ٣ | ١٥ |
| صفحات لها وصف فريد | ٣ | ١٥ |
| صفحات لها بطاقة مشاركة خاصّة | ٠ (كلّها الصورة الشخصية) | ١٥ |
| كتل بيانات منظّمة | ٠ | ٣٠ |
| صفحات موضوعات | ٠ | ٨ |
| تغذية | لا | Atom بالمتن الكامل |

**ما كان غير مرئيّ لزاحف لا ينفّذ جافاسكربت وصار مرئيّاً:**
`/article.html?id=…` (كلا المقالين) · `/project-details.html?id=…` (كلا
المشروعين) · قائمة المقالات في `/articles.html` · قائمة المشاريع في
`/projects.html` · قسم «مختارات» في الرئيسية. أي **كل محتوى الموقع** ما عدا
نصوص الرئيسية الثابتة.

## ٣.٤ دورة المحتوى — مُختبَرة فعلاً

أضفت مقال اختبار إلى `data/articles.json`، شغّلت البناء، فأُنتج بلا تدخّل:
صفحته · بياناته الوصفية (وصفٌ مشتقّ من متنه) · بطاقة مشاركة مرسومة · مدخلٌ
في خريطة الموقع · مدخلٌ في التغذية · بطاقةٌ في الفهرس · بطاقةٌ في الرئيسية ·
جسرٌ بمعرّفه · رابط قصير · **صفحتا موضوع جديدتان** · و**رابط متبادل ظهر في
صفحة مشروع Power BI** لأنهما يتشاركان الوسم.

ثم حذفته وأعدت البناء: كنس البناء ٧ ملفات، ولم يبقَ له أثر واحد في المستودع
(`grep` على كل HTML وXML وJSON: صفر). `data/articles.json` عاد مطابقاً
لما في git.

## ٣.٥ جولة الفحص الشامل — وعطبان كشفتهما

بعد التسليم الأول شغّلت المتصفّح على **كل صفحة عند ثلاثة عروض** (٧٢ فحصاً)
بدل الاكتفاء بعيّنة. كشفت ملاحظتين حقيقيتين، أُصلحتا:

**١. `/favicon.ico` يعود ٤٠٤ من صفحة ٤٠٤.** `404.html` وحدها كانت بلا وسم
`<link rel="icon">` — وبقيّة الصفحات تعلنه — فيسقط المتصفّح إلى
`/favicon.ico` ولا يجده. خطأ ٤٠٤ داخل صفحة الخطأ. أُضيف الوسم.

**٢. تجاوز أفقي ٧٠٣px عند شاشة ٣٧٥px على أربع صفحات موضوعات.** تعقّبته إلى
عطب نائم في `css/article.css`:

```css
.article-footer-more span { … white-space: nowrap; }   /* تخصيص (0,1,1) */
.article-footer-more-title { min-inline-size: 0; }     /* تخصيص (0,1,0) */
```

المحدِّد الأول يشمل عنوان المدخل أيضاً ويغلب الثاني. أي أن **عنوان كل مدخل
في «مقالات أخرى» كان يُطبع بخطّ الميتا ولونها ومقاسها، وبـ`nowrap`.** لم
يظهر الأثر قطّ لأن تلك الكتلة لم تُصيَّر ببيانات حقيقية: مع عنصرين اثنين
يستوعبهما «السابق/التالي» فلا يبقى ما يُدرَج فيها. صفحات الموضوعات أوّل ما
صيّرها — وبداخلها عنوان مشروعٍ من مئة محرف لا يستطيع الالتفاف.

صُحّح المحدِّد إلى `a > span + span` (التسمية الجانبية وحدها)، وأُضيف
`overflow-wrap: anywhere` للعنوان. **صفر بكسل تغيّر على الموقع الحيّ**: هذه
الكتلة لا تُصيَّر فيه أصلاً.

**وملاحظة ثالثة بصرية:** صفحة الموضوع كانت تلفّ قائمتها في `.article-footer`
— وهو مكوّن «تذييل نشر» يفترض أن قبله مقالاً فيحمل `margin-block-start: 64px`
— فتراكم ٢٢٣px من الفراغ الميت تحت العنوان. استُبدل بـ`.articles-grid`،
وهي الحاوية نفسها التي يستعملها فهرس المقالات، فتطابق إيقاع الصفحتين
**بلا سطر CSS واحد جديد**.

بعد الإصلاحات الثلاثة: ٧٢/٧٢ نظيفة، والرئيسية ما زالت **صفر بكسل**.

## ٣.٦ الفحوص الأخرى

- **كل مسار قديم يُقدَّم بـ200** ووجهته موجودة على القرص — الأربعة بالمعرّف
  مفحوصة واحداً واحداً.
- **صفر رابط داخليّ مكسور · صفر صفحة يتيمة · صفر عنوان مكرّر · صفر وصف مكرّر**
  (`check-seo.js` على ٢٤ صفحة و٣٠ كتلة بيانات منظّمة).
- **صفر خطأ في الطرفية** وصفر طلب فاشل على صفحات التفاصيل والفهارس.
- **العربية والاتجاه:** `lang="ar" dir="rtl"` على كل صفحة مولَّدة، وصفر تجاوز
  أفقي عند ٣٧٥px، وفتات الخبز والعنوان والمتن كلّها من اليمين.
- **فلاتر المشاريع تعمل على البطاقات المصيَّرة مسبقاً**: «Python» ⇒ بطاقة
  واحدة، والمنطقة الحيّة تنطق «عُرض ١ من المشاريع»، والعودة إلى «الكل» تستعيد
  الاثنتين.
- **باعث التحديث** رُفع `r29` ← `r30` في كل صفحة (والفاحص يمنع اختلافه).

---

# الجزء ٤ — مهامّك أنت

> مرتّبة بالأولوية. الأولى تحسم قراراً، والثانية تنشر، وما بعدهما يجعل ما
> نُشر يُرى.

---

## المهمّة ١ — احسم الموضعة ✦ الأولوية: أعلى · ٥ دقائق

**ما هي:** الموقع يقول عنك شيئين مختلفين. `<title>` والوصف — أي ما يظهر في
نتيجة بحث Google — يقولان **«محلل بيانات»**. والهيرو وقسم «عني» وبطاقة
المشاركة يقولون **«مطوّر حلول البيانات والذكاء الاصطناعي»**.

**لماذا لا تُؤتمت:** هذا قرار عن نفسك، لا عن الشفرة. أيّهما تريد أن يقرأه
الباحث في سطر النتيجة؟ «محلل بيانات» عبارة يبحث عنها الناس أكثر وتوصّف
وظيفةً قائمة؛ و«مطوّر حلول البيانات والذكاء الاصطناعي» أدقّ لما تفعله فعلاً
وأقلّ ازدحاماً. أنا وحّدت المصدر ولم ألمس القيمة: الموقع الحيّ يقول اليوم ما
كان يقوله بالضبط.

**الخطوات:**

1. افتح `data/site.json`.
2. اذهب إلى كتلة `"positioning"`. ستجد:
   ```json
   "search_role_ar": "محلل بيانات",
   "search_role_en": "Data Analyst",
   "hero_role_ar": "مطوّر حلول البيانات والذكاء الاصطناعي",
   "hero_role_en": "Data & AI Solutions Developer"
   ```
3. **إن اخترت «مطوّر حلول البيانات والذكاء الاصطناعي»** (توصيتي: أدقّ وأقلّ
   ازدحاماً، وهو ما يقوله الموقع في ثلاثة مواضع من أربعة): اجعل
   `search_role_ar` مساوياً لـ`hero_role_ar` حرفاً بحرف، و`search_role_en`
   مساوياً لـ`hero_role_en`. طول العنوان الناتج ٥١ محرفاً — تحت الحدّ.
4. **إن اخترت «محلل بيانات»:** اجعل `hero_role_ar` مساوياً لـ`search_role_ar`
   — وانتبه أن هذا يغيّر النصّ **المرئيّ** تحت اسمك في الهيرو.
5. راجع `"home"` في الملف نفسه: `description_ar` و`og_description_ar` ينبغي
   أن يتّسقا مع ما اخترت.
6. شغّل: `npm run seo`
7. يجب أن يختفي تحذير «الموضعة غير محسومة».

**كيف تتأكّد:** `grep '<title' index.html` يعطي الصياغة الجديدة، و`npm run check`
يمرّ بلا هذا التحذير.

---

## المهمّة ٢ — راجع وانشر ✦ الأولوية: أعلى · ٢٠–٣٠ دقيقة

**ما هي:** كل ما سبق على فرع `seo/static-prerender` **ولم يُدفع ولم يُنشر**.
الموقع الحيّ لم يتغيّر.

**لماذا لا تُؤتمت:** النشر قرارك وحدك، ولأن هذا تغيير في بنية الروابط —
يستحقّ عيناً قبل أن يراه أحد.

**الخطوات:**

1. في الطرفية داخل مجلّد المشروع:
   ```
   git checkout seo/static-prerender
   npm install
   npm run seo
   npm run serve
   ```
2. افتح `http://127.0.0.1:5173` وتصفّح: الرئيسية، ثم «ذُرى»، ثم مشروعاً، ثم
   «بان»، ثم مقالاً. تحقّق من فتات الخبز أعلى الصفحات الفرعية.
3. جرّب رابطاً قديماً: `http://127.0.0.1:5173/article.html?id=e0b1daa47a964f308a4310c2f6494ba2`
   — يجب أن ينقلك إلى المقال فوراً.
4. اضغط زرّ **EN** وتأكّد أن الواجهة تُترجَم والمتن يبقى عربياً.
5. أوقف الخادم (`Ctrl+C`). راجع الفرق:
   ```
   git diff main --stat
   ```
6. حين ترضى:
   ```
   git checkout main
   git merge seo/static-prerender
   git push
   ```
7. انتظر ٢–٣ دقائق (بناء GitHub Pages)، ثم افتح `https://alzobaidi.me/articles/`.

**كيف تتأكّد:** في الطرفية —
`curl -s https://alzobaidi.me/articles/ | grep -c "article-card"`
يجب أن يعطي رقماً أكبر من صفر. إن أعطى صفراً، فالنشر لم يكتمل بعد.

**إن أردت التراجع:** `git revert -m 1 HEAD && git push` يعيد الموقع كما كان.

---

## المهمّة ٣ — Google Search Console ✦ الأولوية: عالية · ١٥ دقيقة

**ما هي:** تسجيل الموقع لدى Google وإرسال خريطة الموقع، حتى يزحف إلى الصفحات
الجديدة خلال أيام بدل أسابيع.

**لماذا لا تُؤتمت:** يتطلّب تسجيل دخول بحسابك وإثبات ملكية النطاق.

**الخطوات:**

1. افتح `https://search.google.com/search-console` وسجّل الدخول بحساب Google.
2. اضغط **Add property** (أو **إضافة موقع**).
3. اختر العمود الأيسر **Domain** (نطاق) — لا **URL prefix**. النطاق يغطّي كل
   الصيغ دفعةً واحدة.
4. اكتب: `alzobaidi.me` (بلا `https://` وبلا `www`). اضغط **Continue**.
5. ستظهر شاشة إثبات الملكية بسجلّ TXT. انسخ القيمة كاملةً — تبدأ بـ
   `google-site-verification=`.
6. افتح لوحة مزوّد النطاق (حيث اشتريت `alzobaidi.me`) ← **DNS**.
7. أضف سجلاً:
   - **Type:** `TXT`
   - **Name / Host:** `@` (أو اتركه فارغاً — يعني الجذر)
   - **Value:** الصقْ ما نسخته
   - **TTL:** الافتراضي
8. احفظ، ثم ارجع إلى Search Console واضغط **Verify**. إن فشل، انتظر ١٠–٣٠
   دقيقة (انتشار DNS) وأعد المحاولة.
9. بعد النجاح: من القائمة اليسرى اختر **Sitemaps**.
10. في خانة **Add a new sitemap** اكتب: `sitemap.xml` — ثم **SUBMIT**.
11. من القائمة اليسرى **URL Inspection**، الصق:
    `https://alzobaidi.me/articles/` واضغط **Request Indexing**. كرّر للرئيسية
    ولـ`https://alzobaidi.me/projects/`.

**بديل إن تعذّر الوصول إلى DNS:** في الخطوة ٣ اختر **URL prefix** واكتب
`https://alzobaidi.me`، ثم اختر طريقة **HTML tag**، وانسخ وسم `<meta>`،
والصقه في `index.html` **بعد** السطر `<!-- seo:head:end -->` مباشرةً — لا
قبله ولا بينه وبين `begin`، فما بين العلامتين يُمحى في كل بناء.

**كيف تتأكّد:** بعد ٢–٣ أيام، **Sitemaps** يعرض الحالة `Success` وعدد
الروابط المكتشفة ١٥. وفي **Pages** يبدأ عدّاد الصفحات المفهرسة بالارتفاع.

---

## المهمّة ٤ — Bing Webmaster Tools ✦ الأولوية: متوسطة · ٥ دقائق

**ما هي:** تسجيل الموقع لدى Bing. يهمّ لأن Bing يغذّي DuckDuckGo وYahoo
و**نتائج البحث في ChatGPT**.

**لماذا لا تُؤتمت:** تسجيل دخول وإثبات ملكية.

**الخطوات:**

1. افتح `https://www.bing.com/webmasters` وسجّل الدخول.
2. اضغط **Import from Google Search Console** — أسرع طريق، ويستورد الملكية
   والخريطة معاً. (أنجز المهمّة ٣ أولاً.)
3. إن فضّلت يدوياً: **Add a site**، اكتب `https://alzobaidi.me`، ثم اختر
   **DNS verification (CNAME)** أو **XML file**، واتّبع التعليمات.
4. بعد التحقّق: **Sitemaps** ← **Submit sitemap** ← الصق
   `https://alzobaidi.me/sitemap.xml`.
5. من القائمة اليسرى **IndexNow** ← فعّله. يُعلم Bing فوراً عند تغيّر صفحة.

**كيف تتأكّد:** **Sitemaps** يعرض الخريطة بحالة `Success`.

---

## المهمّة ٥ — املأ حقول Notion ✦ الأولوية: عالية · ١٠ دقائق للمقال

**ما هي:** الخطّ يبني كل شيء آلياً، لكن جودة ما يبنيه تتبع ما تملؤه. اليوم:
**كلا المقالين بلا وسوم ولا وصف**، فلا يظهران في أي صفحة موضوع ولا يرتبطان
بأي مشروع.

**لماذا لا تُؤتمت:** لا أستطيع اختراع وسمٍ ولا وصفٍ لك — قاعدة هذا العمل ألّا
يُكتب نصّ لم تكتبه أنت.

**الخطوات — لكل مقال في قاعدة المقالات:**

1. افتح قاعدة المقالات في Notion.
2. تأكّد من وجود خاصّية اسمها **`Tags`** ونوعها **Multi-select**. إن لم توجد:
   اضغط **+** في رأس الجدول ← اسمها `Tags` ← **Type: Multi-select**.
3. املأها لكل مقال بـ٢–٤ وسوم. **استعمل نفس الأسماء المستعملة في تصنيفات
   المشاريع** كي يتجسّر المقال بالمشروع. الأسماء القائمة اليوم:
   `Power BI` · `Data Analysis` · `DAX` · `ETL` · `Data Integration` ·
   `Data Quality` · `Python`
   - مثال لمقال «الذكاء الاصطناعي في كتابة الإيميلات»:
     `الذكاء الاصطناعي` · `أدوات العمل` · `الكتابة`
   - مثال لمقال «الهُوية بين وطأة الاعلام»: `الهوية` · `اللغة العربية` ·
     `الإعلام`
4. تأكّد من وجود خاصّية **`Description`** ونوعها **Rich text** (نصّ). املأها
   بجملة أو جملتين، **١٥٠–١٦٠ محرفاً**، بصيغة تجعل من يقرؤها في نتيجة البحث
   ينقر.
   - مثال: `متى يكون الاعتماد على ChatGPT في كتابة الإيميلات مناسباً، ومتى يصير الإيميل نشازاً؟ تصنيفٌ عمليّ للإيميلات، وقاعدتان: الإيجاز والتأثير.`
5. تأكّد من وجود **`Category`** ونوعها **Select** — تظهر تحت عنوان المقال.
6. ضع **غلافاً** للصفحة (أعلى صفحة Notion ← **Add cover**). يصير بطاقة
   المشاركة. بلا غلاف تُرسَم بطاقة من العنوان — مقبولة، والصورة أفضل.
7. **تحت كل صورة في المتن**، اكتب تعليقاً في Notion (اضغط الصورة ← **Caption**).
   يصير النصّ البديل. **٦ صور اليوم بلا تعليق** فأُعلنت زخرفية.
8. للمشاريع: الخاصّية اسمها **`Categories`** (Multi-select) و**`Summary`**
   (Rich text) — نفس المنطق.
9. بعدها: GitHub ← **Actions** ← **Update Articles from Notion** ← **Run workflow**.

**كيف تتأكّد:** بعد انتهاء التشغيلة، افتح `https://alzobaidi.me/topics/` —
يجب أن تظهر موضوعاتك الجديدة، وأن يظهر المقال داخلها. ومحلياً: `npm run build`
ثم تأكّد أن التحذير «عنصراً بلا وسوم» اختفى.

---

## المهمّة ٦ — افحص الرابط العربي في تطبيقات المشاركة ✦ الأولوية: متوسطة · ١٠ دقائق

**ما هي:** التحقّق من أن مسار المقال العربي ينجو من النسخ واللصق في واتساب
ولينكدإن وإكس، وأن معاينة الرابط تظهر.

**لماذا لا تُؤتمت:** يحتاج حساباً وجهازاً حقيقيَّين. فحصت ما يخصّ الخادم
(٢٠٠ للصيغتين، ووسوم OG تصل كاملةً لوكلاء المعاينة الخمسة) — أما سلوك التطبيق
نفسه فلم أفحصه ولم أدّعِ فحصه.

**الخطوات — بعد النشر (المهمّة ٢):**

1. من متصفّح الجوال افتح `https://alzobaidi.me/articles/` واضغط أول مقال.
2. انسخ الرابط من شريط العنوان.
3. الصقه في محادثة واتساب **مع نفسك** (اضغط اسمك في قائمة جهات الاتصال).
4. راقب ثلاثة أشياء:
   - هل صار الرابط **قابلاً للنقر** (أزرق)؟
   - هل ظهرت **بطاقة معاينة** بصورة وعنوان؟
   - هل الرابط **مقطوع** في منتصفه أو مبعثر الاتجاه؟
5. كرّر في **لينكدإن** (منشور مسوّدة، لا تنشره) وفي **إكس**.
6. الآن جرّب **الرابط القصير** للمقال نفسه: `https://alzobaidi.me/a/e0b1daa4/`
   وكرّر الخطوات ٣–٥.

**كيف تتأكّد ومتى تتصرّف:**

- إن نجح الرابط الطويل في الثلاثة ⇒ لا شيء عليك. استعمل المسارات النظيفة.
- **إن أخفق أيٌّ منها** ⇒ استعمل الرابط القصير عند المشاركة اليدوية. وهو
  موجود لكل مقال ومشروع: `alzobaidi.me/a/<أول ٨ محارف من المعرّف>/`
  و`alzobaidi.me/p/…` — تجدها في `data/slugs.json`.
- إن أخفق وأردت **معرّفات لاتينية للجميع**: أخبرني وأنا أبدّل قاعدة الاشتقاق
  في `.github/scripts/lib/seo.js`، وسيكتب البناء جسور تحويل من المعرّفات
  العربية تلقائياً، فلا ينكسر رابط.

---

## المهمّة ٧ — قِس الأداء على الرابط الحيّ ✦ الأولوية: متوسطة · ٥ دقائق

**ما هي:** أرقام Lighthouse في الجزء ٣ محلّية، والقياس المحلّي غير ممثِّل
للإنتاج.

**لماذا لا تُؤتمت:** يحتاج الموقع منشوراً (المهمّة ٢).

**الخطوات:**

1. افتح `https://pagespeed.web.dev`
2. الصق `https://alzobaidi.me/` واضغط **Analyze**.
3. سجّل الأرقام الأربعة من تبويب **Mobile**.
4. كرّر لـ`https://alzobaidi.me/articles/` ولرابط مقال.
5. في قسم **Diagnostics** انظر خصوصاً `Ensure text remains visible during
   webfont load` و`Reduce unused CSS`.

**كيف تتأكّد:** CLS على صفحة المقال يجب أن يكون **٠ أو قريباً منه**. إن كان
أعلى من ٠٫١ فأخبرني — يعني شيئاً في الإنتاج يختلف عمّا قِسته.

**ملاحظة:** الخطوط ٥٤٩ ك.ب، وهي أثقل بند في الصفحة. تجزئتها مهمّة قائمة من
قبل هذا العمل ولم تدخل نطاقه.

---

## المهمّة ٨ — تحقّق من البيانات المنظّمة عند Google ✦ الأولوية: منخفضة · ٥ دقائق

**ما هي:** فحصت الكتل الثلاثين بنيوياً (تُحلَّل، `@context` صحيح، الحقول
الواجبة موجودة). أما فاحص Google الرسمي فيعمل على رابط حيّ فقط.

**لماذا لا تُؤتمت:** الأداة عبر الإنترنت وتتطلّب رابطاً منشوراً.

**الخطوات:**

1. افتح `https://search.google.com/test/rich-results`
2. الصق رابط مقال (مثلاً `https://alzobaidi.me/articles/الهوية-بين-وطأة-الاعلام-وضعف-الشخصية/`)
   واضغط **Test URL**.
3. يجب أن يظهر **Breadcrumbs** و**Articles** بلا أخطاء.
4. كرّر لرابط مشروع وللرئيسية (يجب أن تظهر **Person**).

**كيف تتأكّد:** «0 errors» في أعلى النتيجة. التحذيرات (Warnings) عن حقول
مقترحة غير واجبة مقبولة.

---

## المهمّة ٩ — وسّع وصف الصفحة الرئيسية ✦ الأولوية: منخفضة · ٣ دقائق

**ما هي:** وصف الرئيسية ٥٩ محرفاً، والمساحة المتاحة في نتيجة البحث ١٥٠–١٦٠.
أنت تترك ثلثي السطر فارغاً.

**لماذا لا تُؤتمت:** لا أستطيع اشتقاقه من محتوى — الرئيسية نصّها ثابت وليست
مقالاً، وكتابة وصفٍ لك اختراعٌ لا اشتقاق.

**الخطوات:**

1. أنجز المهمّة ١ أولاً (الوصف يجب أن يتّسق مع الموضعة).
2. افتح `data/site.json` ← كتلة `"home"`.
3. وسّع `description_ar` إلى ١٥٠–١٦٠ محرفاً. اذكر ما تفعله ولمن، وضع الكلمات
   التي يبحث بها الناس. مثال للبنية (بصياغتك أنت):
   `<الدور> — أحوّل البيانات المتفرقة إلى <النتيجة>. مشاريع في <الأدوات> ومقالات في <الموضوعات>.`
4. حدّث `description_en` بالمقابل الإنجليزي.
5. `npm run seo` — يجب أن يختفي تحذير «طول الوصف ٥٩ محرفاً».

**كيف تتأكّد:** `npm run check` بلا تحذير الطول.

---

# الجزء ٥ — ما لم أفعله عمداً

### ١. صفحات إنجليزية حقيقية و`hreflang`

**لم يُفعَل لأنه لا يوجد ما يُشار إليه.** مبدّل EN تبديل نصوص في المتصفّح
(مفحوص، الجزء ٢ §٢). إضافة `hreflang` بلا صفحة مقابلة تصريحٌ كاذب.

**ما يتطلّبه:** ترجمة المتن نفسه، وهي ليست عمل شفرة. عملياً: حقل
`Content_EN` في Notion، ثم توليد `/en/articles/<slug>/` في البناء، ثم
`hreflang` متبادل (`ar-SA` ⇄ `en` ⇄ `x-default`) وcanonical مستقلّ لكل
نسخة. البنية جاهزة لذلك — `chrome.js` يقبل اللغة معاملاً — وينقص المحتوى.
**توصيتي:** ابدأ بترجمة مقالين فقط وقِس، بدل ترجمة كل شيء دفعةً.

### ٢. تجزئة الخطوط

٥٤٩ ك.ب من ١٥ ملفاً، بلا `unicode-range`، أي لا يستطيع المتصفّح تخطّي واحد
منها. هي أثقل بند في وزن الصفحة. **خارج نطاق هذا العمل** — تمسّ التصميم
والطباعة، والبند ٢ يمنع مسّهما. مهمّة قائمة من قبل.

### ٣. قائمة الجوال لا تُغلق

> **لم يعد قائماً — أُصلح في ملحق ٢٠٢٦-٠٨-٢١ أدناه.** ما يلي هو نصّ التقرير
> الأصلي، يُترك للسجلّ.

عطبٌ قائم قبل هذا العمل: `main.js` يضع `inert` على كل أبناء `body` عدا لوح
القائمة، و`.navbar` أحدهم — وفيه زرّ الإغلاق نفسه. فيصير ظاهراً وميتاً.
**لم ألمسه** لأنه لا صلة له بالظهور في البحث، والمهمّة محدّدة النطاق. إصلاحه
سطر واحد: استثناء `.navbar` من الحلقة. أخبرني وأفعل.

### ٤. تحويل من جهة الخادم (301)

**غير ممكن على GitHub Pages.** لا خادم يُوجَّه. المتاح هو `canonical` +
`meta refresh` + تحويل بجافاسكربت، وقد استُعملت الثلاثة. البديل الحقيقي وضع
Cloudflare أمام النطاق واستعمال Redirect Rules — تحسينٌ لا شرط، ويضيف طبقة
تشغيلية.

### ٥. `SearchAction` في `WebSite`

لا بحث داخل الموقع، فالتصريح به إعلان ميزة غير موجودة. متى أُضيف بحث، تُضاف
الكتلة في `schemaWebsite()` في `build-site.js`.

### ٦. IndexNow آلياً

الخطّ يبني ويودِع لكنه لا ينادي واجهة IndexNow بعد النشر. **تفعيله من لوحة
Bing (المهمّة ٤ خطوة ٥) يكفي**، ولا يستحقّ سرّاً إضافياً في المستودع.

### ٧. تحسين البطاقات وأزرار الفلترة كروابط زحف

أزرار فلترة المشاريع أزرارٌ لا روابط، فلا يزحف إليها زاحف. **مقصود:** صفحات
الموضوعات تؤدّي الدور نفسه وهي روابط حقيقية مفهرَسة. وتحويل الأزرار إلى
روابط كان سيغيّر التصميم (خطّ تحت النصّ) ويكرّر المحتوى.

### ٨. كتابة أوصاف أو نصوص بديلة نيابةً عنك

قاعدة صارمة في هذا العمل. ما لم يوجد له مصدر في `data/` تُرك فارغاً وأُدرج
في المهامّ (المهمّة ٥). ٦ صور بلا نصّ بديل حقيقي، ومقالان بلا وسوم ولا وصف —
كلّها معلَّمة في مخرَج البناء بأسمائها.

---

## ملحق — الأوامر

```bash
npm install     # مرّة واحدة
npm run seo     # ابنِ ثم افحص
npm run build   # ابنِ
npm run check   # افحص (يفشل عند أي انحدار)
npm run serve   # خادم محلي على 5173
```

التفصيل الكامل للخطّ وجدول «أيّ حقل في Notion يغذّي أيّ وسم» في
[`docs/SEO.md`](docs/SEO.md).

---
---

# ملحق ٢٠٢٦-٠٨-٢١ — سدّ الثغرات التشغيلية

جولة ثالثة على الفرع نفسه. لا محتوى جديداً ولا صفحات جديدة: خمسة مواضع كان
الخطّ يعمل فيها ويطالب صاحبه بعملٍ يدويّ، أو يترك خطراً بلا حارس.

**الحالة: مُودَع على `seo/static-prerender`. لم يُنشَر شيء.**

---

## أ — ما تغيّر

### ١. باعث التحديث يُرفع في مكان واحد

**كان:** `docs/SEO.md` §9 يطلب رفعه في `data/site.json` ثم يدوياً في
`index.html` ثم في `404.html`. ونسيان واحدٍ يُفشل البناء. الفشل صحيح؛
الخطوة اليدوية هي العلّة.

**صار:** منطقتان مُدارتان جديدتان — `seo:assets` (سكربت الانتقال + تحميل
الخطوط المسبق + وسوم الأنماط) و`seo:scripts` (سكربتات آخر الجسد) — في
الصفحتين اليدويتين، على نمط `seo:head` القائم. القوائم في `MANAGED_ASSETS`
داخل `build-site.js`، والباعث من `site.json`.

**مقيس:** رفعتُ `cacheBuster` من `r30` إلى `r31` في `data/site.json` **وحده**
وشغّلت `npm run seo`. النتيجة: **١٥٠ مرجعاً عبر ٢٤ صفحة، كلّها `r31`، بقيمة
واحدة** — بلا لمس أي HTML.

**فحص التباين في `check-seo.js` باقٍ كما هو**، لم يُضعَّف ولم يُحذف. جرّبته
بتغيير مرجع واحد يدوياً إلى `r29` فأفشل الفحص كما ينبغي. صار حارساً على
منطقة تالفة بدل أن يكون خطوة على عاتق صاحب الموقع.

### ٢. المساران مُزاحان، والسجلّ يُدمَج

**كان:** كلاهما على `0 3 * * *` — الدقيقة نفسها — وكلاهما يكتب
`sitemap.xml` و`feed.xml` و`data/slugs.json` و`assets/og/*` وأشجار الصفحات.
`git pull --rebase` يمنع فشل الدفع، ولا يمنع كتابةً فوق كتابة.

**صار:** ثلاثة خطوط دفاع مرتّبة —

1. المشاريع إلى `30 3 * * *`، والمقالات تبقى `0 3 * * *`.
2. `writeLedger()` يُعيد قراءة `data/slugs.json` **لحظة الكتابة لا لحظة
   البدء**، ويدمج: مدخل على القرص وغائب هنا **يبقى**؛ ومدخل في الجهتين
   يفوز معرّفه من هذه التشغيلة وتُجمَع أسماؤه المهجورة من الجهتين. **لا
   يُحذف مدخل أبداً كأثر جانبي.**
3. `git pull --rebase --autostash` كما هو.

**مقيس بمحاكاة التسابق الحقيقي** (`writeLedger` صُدِّر لهذا الغرض): «أ»
يقرأ السجلّ، ثم يكتب «ب» مدخلاً جديداً واسماً مهجوراً على القرص، ثم يكتب
«أ». ستّ فحوص، كلّها ✓:

| الفحص | النتيجة |
|---|---|
| مدخل «أ» موجود | ✓ |
| مدخل «ب» نجا رغم أن «أ» لم يكن يعرفه | ✓ |
| الاسم المهجور الذي كتبه «ب» لم يسقط | ✓ |
| الاسم المهجور الذي يحمله «أ» باقٍ | ✓ |
| كل المدخلات الحقيقية باقية | ✓ |
| المفاتيح مرتّبة ترتيباً حتمياً | ✓ |

### ٣. قسم تراجع في الملف الدائم

`docs/SEO.md` كان يغطّي فشل البناء وحده. مسار «نشرتُ وانكسر» كان يعيش في هذا
التقرير — وهو وثيقة لحظة، لا مرجع.

**أُضيف §11 «التراجع»**: أربع حالات، كلٌّ منها **عرَض ← أمر ← كيف تتأكّد**:
(أ) عكس إيداع دمج ودفعه · (ب) استعادة الصفحتين اليدويتين إذا تلفت مناطقهما ·
(ج) إعادة اشتقاق الصفحات حين تخرج عن التزامن مع `data/` · (د) فحص أن الموقع
الحيّ عاد فعلاً — بـ`curl` بنتيجة متوقّعة، لا «انظر إليه».

**كل أمر في القسم شُغِّل فعلاً** قبل توثيقه (ما عدا الأوامر التي تمسّ النطاق
الحيّ، وهي غير منشورة بعد). ومنها إعادة الاشتقاق من الصفر: حذفتُ
`articles/ projects/ topics/ a/ p/ assets/og/` كاملةً وشغّلت `npm run seo`
فعادت الحالة مطابقة.

### ٤. تراكم الجسور موثَّق

`docs/SEO.md` §6: الجسور تتراكم **بالتصميم**، ولا تُحذف آلياً ولن تُحذف —
حذف جسر يحوّل رابطاً منشوراً إلى ٤٠٤. الكلفة ضئيلة (ملف صغير، `noindex`،
خارج الخريطة). أُضيف أمر يعدّها، وكيفية حذف واحد **عمداً** وما ينكسر حين
تفعل. **ولم أبنِ آلية تقليم** — كما طُلب صراحةً.

### ٥. قائمة الجوال تُغلق

`main.js` كان يضع `inert` على كل أبناء `body` عدا اللوح، و`.navbar` أحدهم —
وفيه زرّ الإغلاق. فكان الزرّ ظاهراً وميتاً، ولا Escape على اللمس، واللوح
يغطّي الشاشة فلا «خارج» يُنقر. المخرج الوحيد كان الانتقال إلى صفحة أخرى.

الاستثناء يُشتقّ من الزرّ نفسه — `mobileToggle.closest('.navbar')` — لا من
معرّف مكتوب. القاعدة: **العنصر الذي يحوي أداة إغلاق الطبقة لا يُخمَد معها.**

**مقيس في متصفّح حقيقي عند ٣٧٥px**، على الرئيسية وعلى صفحة مولَّدة:

| الفحص | النتيجة |
|---|---|
| اختبار الإصابة عند مركز زرّ ✕ يعيد الزرّ نفسه | ✓ |
| النقرة تصل إلى المعالج | ✓ |
| القائمة تُغلق، و`aria-expanded` ← `false` | ✓ |
| قفل التمرير يُفكّ | ✓ |
| رابط في المتن داخل `[inert]` أثناء الفتح | ✓ |
| اختبار الإصابة فوق المتن يعيد اللوح لا الرابط | ✓ |
| محطّات Tab: ٧ فقط — ٣ في النافبار و٤ في اللوح، صفر خارجهما | ✓ |
| Escape يُغلق ويعيد التركيز إلى الزرّ | ✓ |
| لا بقايا `inert` بعد الإغلاق | ✓ |

المُخمَد أثناء الفتح: `skip-link` · `main` · `footer` · شريط تقدّم القراءة.
غير المُخمَد: النافبار واللوح وحدهما.

---

## ب — قرارات اتُّخذت بلا سؤال

**١. منطقتان للأصول لا واحدة.** المطلوب «منطقة ثالثة»، لكن وسوم الأصول تسكن
موضعين من المستند: الأنماط في `<head>` والسكربتات في آخر `<body>`. ومنطقة
واحدة لا تمتدّ عبر جسد الصفحة كلّه. فصارتا `seo:assets` و`seo:scripts` —
نمطٌ واحد، موضعان.

**٢. مسارات `index.html` صارت مطلقة من الجذر.** كانت `css/style.css` نسبيّة
وصارت `/css/style.css`. الصفحة في الجذر فالنتيجة واحدة، لكن مولّد المنطقة
واحد للصفحتين، و`404.html` **تحتاج** المطلق (تُقدَّم لأي مسار مفقود مهما
عمق). مسارٌ واحد في الشفرة خير من فرعين. مُتحقَّق: الرئيسية صفر بكسل.

**٣. الباعث `r30` ← `r31` مُثبَّت لا مؤقّت.** `main.js` تغيّر في هذه الجولة،
فالقاعدة نفسها توجب الرفع. ولم أُعِده بعد فحص الانتشار.

**٤. مفاتيح `slugs.json` مرتّبة بالمفتاح لا بترتيب العرض.** ترتيب العرض
يتبدّل مع كل تغيير تاريخ فيُنتج فرقاً وهمياً في git ويضاعف احتمال التعارض
بين المسارين. الترتيب بالمفتاح ثابت. أثره لمرّة واحدة: إعادة ترتيب ٤ مدخلات.

**٥. `writeLedger` صار مُصدَّراً، والبناء خلف `require.main === module`.**
دمج السجلّ هو الموضع الوحيد الذي يتنافس عليه مساران، ويستحقّ فحصاً مباشراً
لا استنتاجاً. الحارس يُبقي `node build-site.js` يعمل كما كان.

**٦. `patchRegions` شُدِّد ولم يُضعَّف.** كان يتحقّق من وجود العلامتين؛ صار
يرفض أيضاً العلامة المكرّرة والعلامتين المعكوستين — كلاهما منطقة تالفة، وأي
منهما كان سيكتب في موضع مُخمَّن. **الحرّاس الأربعة مُجرَّبون واحداً واحداً**
(مفقودة · مكرّرة · معكوسة · تباين الباعث) وكلّها أوقفت البناء أو الفحص.
وأُضيف إليه أن إزاحة علامة الإغلاق تتبع إزاحة الفتح، فيبقى الترميز مرتّباً.

**٧. الاستثناء من `inert` بالبنية لا بالاسم.** `mobileToggle.closest('.navbar')`
بدل `document.getElementById('navbar')`: يُشتقّ من الزرّ الذي يجب أن يبقى
حيّاً، فلا ينكسر لو تغيّر معرّف أو انتقل الزرّ.

**٨. لم أبنِ تقليماً للجسور.** طُلب صراحةً ألّا أفعل. وُثّقت وحسب.

---

## ج — ما قِيس

| القياس | النتيجة |
|---|---|
| انتشار الباعث من ملف واحد | ١٥٠ مرجعاً · قيمة واحدة · صفر لمسة يدوية |
| محاكاة تسابق السجلّ | ٦/٦ ✓ |
| قائمة الجوال (صفحتان × ٩ فحوص) | ١٨/١٨ ✓ |
| حرّاس البناء الأربعة | ٤/٤ توقّفت كما ينبغي |
| `npm run seo` | بلا أخطاء · تنبيهان قائمان (مهمّتاك ١ و٩) |
| حتميّة البناء | بناء ثانٍ ⇒ صفر ملف متغيّر |
| فحص المتصفّح الشامل | ٢٤ صفحة × ٣ عروض = ٧٢ ✓ |
| **الفرق البصري عن الحالة السابقة** | **٨ أنواع صفحات · صفر بكسل في كلّها** |

**عن قياس الفرق البصري:** أول قياس أظهر ٢٢٩٧ بكسلاً على صفحة المقال —
شريط تقدّم القراءة. أعدتُ الالتقاط ثلاث مرّات لكل جانب: الجانبان متطابقان
داخلياً (صفر) ومتطابقان معاً (صفر). كان أثر التقاطٍ بارد على خادم بدأ
لتوّه، لا فرقاً في المخرَج. والترميز نفسه متطابق حرفياً بعد تحييد الباعث.

---

## د — أثر هذا على مهامّك

### أُلغيت

**رفع باعث التحديث يدوياً في `index.html` و`404.html`.** كانت خطوة متكرّرة
عند كل تعديل على CSS أو JS، موثّقة في `docs/SEO.md` §9. لم تعد موجودة:
ارفع `cacheBuster` في `data/site.json` وشغّل `npm run seo`.

**«إصلاح قائمة الجوال» في الجزء ٥ §٣.** كانت مُدرَجة فيما لم أفعله. أُصلحت.

### لم تتغيّر

المهامّ التسع في **الجزء ٤** كلّها كما هي: الموضعة · المراجعة والنشر ·
Search Console · Bing · حقول Notion · فحص الرابط العربي في تطبيقات المشاركة ·
PageSpeed · فاحص البيانات المنظّمة · وصف الرئيسية.

**تنبيه على المهمّة ٣ (Search Console):** الخطوة البديلة تطلب لصق وسم
`<meta>` للتحقّق **بعد** `<!-- seo:head:end -->`. هذا ما زال صحيحاً — وصار
أهمّ: في `index.html` الآن **خمس** مناطق مُدارة، وما بين علامات أيّ منها
يُمحى في كل بناء. الصقه خارجها جميعاً.

### قاعدة جديدة تعرفها

**إضافة ملف CSS أو JS إلى `index.html` أو `404.html` لم تعد تحريراً للملف.**
أضفه إلى `MANAGED_ASSETS` في `.github/scripts/build-site.js` ثم `npm run seo`.
تحريرُ الملف مباشرةً يُمحى في البناء التالي. (`docs/SEO.md` §8.)

### لا مهمّة يدوية جديدة

لا شيء في هذه الجولة يضيف عملاً عليك.
